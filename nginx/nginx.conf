events {}

http {
    upstream backend {
        server localhost:9090;
        server localhost:9091;
    }

    server {
        listen 8000;

        location / {
            proxy_pass http://backend;
        }
    }
}